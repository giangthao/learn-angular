import { TotalParticipantToStringPipe } from './total-participant-to-string.pipe';

describe('TotalParticipantToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalParticipantToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
